import { Box, Button } from "@mui/material";

const BoxComponent = () => {
  return (
    <Box className={"mui-class"} component={"section"}>
      <h1>Box Component</h1>
    </Box>
  );
};

export default BoxComponent;
