# PART - 1
1. How to write a "Hello World" program using pure HTML
2. How to write a "Hello World" program using Javascript and display it in Chrome Browser
3. All about content delivery network links
    3.1. What is meant by?
    3.2. why do we need?
    3.3. what are two types of links available for react?
        3.1.1. react.development.js
        3.1.2. react-dom.development.js
4. What are advantages and disadvantages of using CDN?
5. What is meant by crossorigin attribute in CDN links?

# Importing react code into your project via CDN Links
When you actually import the official CDN links into your project space, you will get all the source related to React
to leverage all the concepts of React.

After importing the CDN links into your project, go to DevTool of Chrome and type "React" and Enter to see magic of React.
The above method will give you the assurance of React

The source code available via CDN links are written by facebook(only for React here)
Written Language Of Source Code: pure vanilla javascript

# PART - 2
1. How to create a "Hello World" using React
    1.1. What is React.createElement() and how many arguments it could be received?
        React.createElement is actually creating an object, not pure HTML tags.
    1.2. What is ReactDOM.createRoot() method?
    1.3. what is render() method? 
        Responsible for accepting the React Element as an argument and converting the object into actual h1 element and put into the DOM tree so the browser understands.
    1.4. How will you add attributes in the tags in React?
2. What is the most costly/expensive operation in the webpage?
    DOM Manipulation - Because When your webpage is more interactive (DOM tree is getting changed very often, Now these libraries are introduced to optimize that.)

# PART - 4
1. How to create hierarchial structure React Elements in React?
2. How to create a siblings React Elements?

# PART - 5
1. Library vs. Framework
2. React could be applied to section of your website like header etc. you can even apply react to an existing  project.
3. Frameworks means the whole applications need to be created with that framework only.